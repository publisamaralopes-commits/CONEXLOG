{
  "name": "espaco-de-trabalho",
  "version": "0.0.0",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "pnpm run typecheck && pnpm run build:all",
    "typecheck": "tsc --noEmit",
    "build:all": "pnpm -r --if-present run build",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}